{{# with leave ~}}
  {{~# if this.get_leave_type_name ~}}{{~ this.get_leave_type_name }} {{/if~}}
  from {{# with this.get_start_leave_day ~}}
    {{~ as_date this.date ~}}
    {{~# if this.is_morning_leave}} (morgens){{/if~}}
    {{~# if this.is_afternoon_leave}} (nachmittags){{/if~}}
  {{~/with}} to {{# with this.get_end_leave_day ~}}
    {{~ as_date this.date ~}}
    {{~# if this.is_morning_leave}} (morgens){{/if~}}
    {{~# if this.is_afternoon_leave}} (nachmittags){{/if~}}
  {{~/with~}}
{{~/with~}}
